diff --git a/std/src/sys/alloc/mod.rs b/std/src/sys/alloc/mod.rs
--- a/std/src/sys/alloc/mod.rs
+++ b/std/src/sys/alloc/mod.rs
@@ -57,6 +57,10 @@ cfg_select! {
         mod unix;
         use unix as imp;
     }
+    target_os = "jvm" => {
+        mod jvm;
+        use jvm as imp;
+    }
     target_os = "windows" => {
         mod windows;
         use windows as imp;
diff --git a/std/src/sys/io/error/mod.rs b/std/src/sys/io/error/mod.rs
--- a/std/src/sys/io/error/mod.rs
+++ b/std/src/sys/io/error/mod.rs
@@ -36,6 +36,7 @@ cfg_select! {
         target_family = "wasm",
         target_os = "zkvm",
         target_os = "trusty",
+        target_os = "jvm",
     ) => {
         mod generic;
         pub use generic::*;
diff --git a/std/src/sys/random/mod.rs b/std/src/sys/random/mod.rs
--- a/std/src/sys/random/mod.rs
+++ b/std/src/sys/random/mod.rs
@@ -9,6 +9,10 @@ cfg_select! {
         mod apple;
         pub use apple::fill_bytes;
     // Others, in alphabetical ordering.
+    }
+    target_os = "jvm" => {
+        mod jvm;
+        pub use jvm::fill_bytes;
     }
     any(
         target_os = "dragonfly",
diff --git a/std/src/sys/time/mod.rs b/std/src/sys/time/mod.rs
--- a/std/src/sys/time/mod.rs
+++ b/std/src/sys/time/mod.rs
@@ -3,6 +3,10 @@ cfg_select! {
         mod hermit;
         use hermit as imp;
     }
+    target_os = "jvm" => {
+        mod jvm;
+        use jvm as imp;
+    }
     target_os = "motor" => {
         use moto_rt::time as imp;
     }
diff --git a/std/src/sys/sync/futex/mod.rs b/std/src/sys/sync/futex/mod.rs
--- a/std/src/sys/sync/futex/mod.rs
+++ b/std/src/sys/sync/futex/mod.rs
@@ -1,4 +1,8 @@
 cfg_select! {
+    target_os = "jvm" => {
+        mod jvm;
+        pub use jvm::*;
+    }
     any(
         target_os = "linux",
         target_os = "android",
diff --git a/std/src/sys/sync/mutex/mod.rs b/std/src/sys/sync/mutex/mod.rs
--- a/std/src/sys/sync/mutex/mod.rs
+++ b/std/src/sys/sync/mutex/mod.rs
@@ -12,5 +12,6 @@ cfg_select! {
         target_os = "hermit",
         all(target_os = "wasi", target_env = "p3"),
+        target_os = "jvm",
     ) => {
         mod futex;
         pub use futex::Mutex;
diff --git a/std/src/sys/sync/rwlock/mod.rs b/std/src/sys/sync/rwlock/mod.rs
--- a/std/src/sys/sync/rwlock/mod.rs
+++ b/std/src/sys/sync/rwlock/mod.rs
@@ -13,5 +13,6 @@ cfg_select! {
         target_os = "hermit",
         target_os = "motor",
         all(target_os = "wasi", target_env = "p3"),
+        target_os = "jvm",
     ) => {
         mod futex;
         pub use futex::RwLock;
diff --git a/std/src/sys/sync/condvar/mod.rs b/std/src/sys/sync/condvar/mod.rs
--- a/std/src/sys/sync/condvar/mod.rs
+++ b/std/src/sys/sync/condvar/mod.rs
@@ -13,5 +13,6 @@ cfg_select! {
         target_os = "hermit",
         all(target_os = "wasi", target_env = "p3"),
+        target_os = "jvm",
     ) => {
         mod futex;
         pub use futex::Condvar;
diff --git a/std/src/sys/sync/once/mod.rs b/std/src/sys/sync/once/mod.rs
--- a/std/src/sys/sync/once/mod.rs
+++ b/std/src/sys/sync/once/mod.rs
@@ -20,5 +20,6 @@ cfg_select! {
         target_os = "hermit",
         all(target_os = "wasi", target_env = "p3"),
+        target_os = "jvm",
     ) => {
         mod futex;
         pub use futex::{Once, OnceState};
diff --git a/std/src/sys/thread_local/mod.rs b/std/src/sys/thread_local/mod.rs
--- a/std/src/sys/thread_local/mod.rs
+++ b/std/src/sys/thread_local/mod.rs
@@ -107,6 +107,7 @@ pub(crate) mod guard {
         any(
             target_os = "hermit",
             target_os = "xous",
+            target_os = "jvm",
         ) => {
             // `std` is the only runtime, so it just calls the destructor functions
             // itself when the time comes.
@@ -172,5 +173,15 @@ pub(crate) mod key {
             use xous::{create, destroy};
         }
+        target_os = "jvm" => {
+            mod jvm;
+            mod racy;
+            #[cfg(test)]
+            mod tests;
+            pub(super) use jvm::{Key, get, set};
+            pub(crate) use jvm::destroy_tls;
+            pub(super) use racy::LazyKey;
+            use jvm::{create, destroy};
+        }
         target_os = "motor" => {
             mod racy;
             #[cfg(test)]
diff --git a/std/src/sys/args/mod.rs b/std/src/sys/args/mod.rs
--- a/std/src/sys/args/mod.rs
+++ b/std/src/sys/args/mod.rs
@@ -10,4 +10,5 @@
     target_os = "wasi",
     target_os = "xous",
+    target_os = "jvm",
 ))]
 mod common;
@@ -17,3 +18,7 @@ cfg_select! {
+    target_os = "jvm" => {
+        mod jvm;
+        pub use jvm::*;
+    }
     any(
         all(target_family = "unix", not(any(all(target_family = "wasm", target_os = "linux"), target_os = "espidf", target_os = "vita"))),
         target_os = "hermit",
diff --git a/std/src/sys/env/mod.rs b/std/src/sys/env/mod.rs
--- a/std/src/sys/env/mod.rs
+++ b/std/src/sys/env/mod.rs
@@ -12,4 +12,5 @@
     target_os = "wasi",
     target_os = "xous",
+    target_os = "jvm",
 ))]
 mod common;
@@ -17,4 +18,8 @@
 cfg_select! {
+    target_os = "jvm" => {
+        mod jvm;
+        pub use jvm::*;
+    }
     target_family = "unix" => {
         mod unix;
         pub use unix::*;
diff --git a/std/src/sys/stdio/mod.rs b/std/src/sys/stdio/mod.rs
--- a/std/src/sys/stdio/mod.rs
+++ b/std/src/sys/stdio/mod.rs
@@ -3,4 +3,8 @@
 cfg_select! {
+    target_os = "jvm" => {
+        mod jvm;
+        pub use jvm::*;
+    }
     any(target_family = "unix", target_os = "hermit", target_os = "wasi") => {
         mod unix;
         pub use unix::*;
diff --git a/std/src/sys/pal/mod.rs b/std/src/sys/pal/mod.rs
--- a/std/src/sys/pal/mod.rs
+++ b/std/src/sys/pal/mod.rs
@@ -21,6 +21,10 @@ cfg_select! {
         mod hermit;
         pub use self::hermit::*;
     }
+    target_os = "jvm" => {
+        mod jvm;
+        pub use self::jvm::*;
+    }
     target_os = "motor" => {
         mod motor;
         pub use self::motor::*;
diff --git a/std/src/sys/thread/mod.rs b/std/src/sys/thread/mod.rs
--- a/std/src/sys/thread/mod.rs
+++ b/std/src/sys/thread/mod.rs
@@ -5,6 +5,10 @@ cfg_select! {
         mod unsupported;
         pub use unsupported::{current_os_id, set_name};
     }
+    target_os = "jvm" => {
+        mod jvm;
+        pub use jvm::*;
+    }
     target_os = "motor" => {
         mod motor;
         pub use motor::*;
diff --git a/std/src/sys/sync/thread_parking/mod.rs b/std/src/sys/sync/thread_parking/mod.rs
--- a/std/src/sys/sync/thread_parking/mod.rs
+++ b/std/src/sys/sync/thread_parking/mod.rs
@@ -15,5 +15,6 @@ cfg_select! {
         target_os = "hermit",
         all(target_os = "wasi", target_env = "p3"),
+        target_os = "jvm",
     ) => {
         mod futex;
         pub use futex::Parker;
diff --git a/std/src/sys/exit.rs b/std/src/sys/exit.rs
--- a/std/src/sys/exit.rs
+++ b/std/src/sys/exit.rs
@@ -1,3 +1,9 @@
+#[cfg(target_os = "jvm")]
+unsafe extern "C" {
+    #[link_name = "jvm:static:org/rustlang/runtime/RuntimeSupport:exit"]
+    fn jvm_exit(code: i32) -> !;
+}
+
 cfg_select! {
     target_os = "linux" => {
         /// Mitigation for <https://github.com/rust-lang/rust/issues/126600>
@@ -105,4 +111,7 @@ pub fn exit(code: i32) -> ! {
     cfg_select! {
+        target_os = "jvm" => {
+            unsafe { jvm_exit(code) }
+        }
         target_os = "hermit" => {
             unsafe { hermit_abi::exit(code) }
         }
diff --git a/std/build.rs b/std/build.rs
--- a/std/build.rs
+++ b/std/build.rs
@@ -18,4 +18,5 @@ fn main() {
     println!("cargo:rustc-check-cfg=cfg(restricted_std)");
     if target_os == "linux"
+        || target_os == "jvm"
         || target_os == "android"
         || target_os == "netbsd"
